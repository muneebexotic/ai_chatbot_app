<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    

    <!-- The microphone. R4.1.2 asks for it once, with a plain explanation of
         why, before the first session. Recognition runs on-device and R4.2.7
         is explicit that no audio leaves the phone: this permission buys the
         transcript, not an upload. -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <uses-permission android:name="com.android.vending.BILLING" />

    <!-- REMOVED IN MILESTONE 4: CAMERA, READ/WRITE_EXTERNAL_STORAGE,
         READ_MEDIA_IMAGES, and the camera hardware feature.
         Nothing in `lib/` has referenced any of them since §2.2 cut image
         generation and Milestone 3 dropped `image_picker` from pubspec. A
         permission the app declares but never uses still appears in the Play
         data-safety form and on the store listing, so it is a claim about the
         product that is not true. §5.4 image *understanding* may add
         READ_MEDIA_IMAGES back if it ships; it will do so with the code that
         needs it. -->


    <application
        android:label="SpeakWise"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme" />
            
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- OAuth return leg (PRD §9.2). Google redirects to Supabase,
                 Supabase redirects here, and this filter is what lets Android
                 hand the callback back to the app instead of leaving the user
                 staring at a browser tab.

                 The scheme is the applicationId, so it cannot collide with
                 another app on the device. It must match the Redirect URL
                 registered on the Supabase project exactly, including the
                 trailing slash on the host path:
                     com.muscodes.speakwise://login-callback/

                 `singleTop` above means the running activity is reused rather
                 than a second one being stacked on the return trip. -->
            <intent-filter android:autoVerify="false">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data
                    android:scheme="com.muscodes.speakwise"
                    android:host="login-callback" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>

    <!-- Android 11 package visibility (API 30+). A manifest may contain exactly
         ONE <queries> element, so the speech entries live here beside the
         Flutter default rather than in a second block near the permissions.

         Without the two audio intents below, `resolveActivity` / `bindService`
         for the system speech and text-to-speech engines returns nothing, and
         `speech_to_text` and `flutter_tts` report "no recogniser available" or
         silently do nothing on a phone that has both installed. Invisible
         below API 30 and total above it, which is the worst shape a bug can
         have — and it would break the two packages the whole business model
         rests on (§3: a spoken minute costs zero because both run on-device). -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT" />
            <data android:mimeType="text/plain" />
        </intent>
        <intent>
            <action android:name="android.speech.RecognitionService" />
        </intent>
        <intent>
            <action android:name="android.intent.action.TTS_SERVICE" />
        </intent>
    </queries>
</manifest>
